const max = (x, y) => x > y ? x : y;

console.log(`O maior número é ${max(10, 5)}`);

const paisagem = (largura, altura) => largura > altura;

console.log(`A paisagem está em modo retrato? ${paisagem(1920, 1080)}`);